<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sinhvien.orderdrinkapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/coffee_icon"
        android:label="TraSuaAnVat"
        android:roundIcon="@drawable/bac2"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderDrinkApp">
        <activity android:name=".Activities.ThongKeActivity"></activity>
        <activity android:name=".Activities.addNhanVienActivity" />
        <activity
            android:name=".Activities.ChinhSuaBanActivity"
            android:label="Sửa bàn ăn"
            android:theme="@style/PopUpActivity" />
        <activity android:name=".Activities.ThanhToanActivity" />
        <activity
            android:name=".Activities.MenuActivity"
            android:label="số lượng món"
            android:theme="@style/PopUpActivity" />
        <activity android:name=".Activities.ThemLoaiActivity" />
        <activity android:name=".Activities.ThemMenuActivity" />
        <activity
            android:name=".Activities.addBangActivity"
            android:label="@string/addTable"
            android:theme="@style/PopUpActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.XacNhanDKActivity" />
        <activity android:name=".Activities.WelcomeActivity" />
        <activity android:name=".Activities.DangKyActivity" />
        <activity android:name=".Activities.MapActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>